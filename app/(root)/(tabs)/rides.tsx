import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text>History</Text>
    </SafeAreaView>
  );
};

export default Rides;
